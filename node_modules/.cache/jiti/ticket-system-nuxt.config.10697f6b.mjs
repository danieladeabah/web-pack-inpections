"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0; // https://nuxt.com/docs/api/configuration/nuxt-config
var _default = exports.default = defineNuxtConfig({
  compatibilityDate: "2025-07-17",
  devtools: { enabled: true },
  srcDir: "src/",
  modules: ["@nuxtjs/tailwindcss", "@pinia/nuxt"],
  css: ["~/assets/css/main.css"],
  app: {
    head: {
      title: `${process.env.APP_NAME || "MENOSOFTS"} ${
      process.env.APP_DESCRIPTION || "Ticket System"}`,

      meta: [
      {
        name: "description",
        content:
        "A modern queue management system for seamless flow and efficiency."
      },
      { name: "viewport", content: "width=device-width, initial-scale=1" }]

    }
  },
  runtimeConfig: {
    jwtSecret: process.env.JWT_SECRET || "your-secret-key",
    SMS_API: process.env.SMS_API,
    public: {
      apiBase: "/api",
      appName: process.env.APP_NAME || "Ticket System",
      appDescription: process.env.APP_DESCRIPTION || "Hospital Ticket System"
    }
  }
}); /* v9-1c5199882ea94706 */
